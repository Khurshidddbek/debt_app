name: qarz_app
description: A new Flutter project.


version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2
  iconsax: ^0.0.8
  animate_do: ^2.0.0
  firebase_core: ^1.7.0
  fluttertoast: ^8.0.8
  shared_preferences: ^2.0.7
  firebase_auth: ^3.1.3
  provider: ^6.0.1
  flutter_svg_provider: ^0.1.10
  align_positioned: ^3.0.0
  ##flutter_plus: ^1.0.0 ## Error. Why? - I don't know

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true

  assets:
    - assets/images/authentication/
    - assets/images/user_avatars/

  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf